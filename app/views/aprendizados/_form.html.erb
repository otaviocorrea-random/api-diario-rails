<%= form_with(model: aprendizado, local: true) do |form| %>
  <% if aprendizado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aprendizado.errors.count, "error") %> prohibited this aprendizado from being saved:</h2>

      <ul>
        <% aprendizado.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
